// method chaining = calling one method after another
//                   in one continious line of code

let userName = " tolga";

let letter = userName.slice(0, 3).toUpperCase().trim();

console.log(letter);